name: Build and Test Pull Requests

on:
  pull_request:
    branches:
    - master

jobs:
  # firefoxtests:
  #   name: "Build and Test Pull Requests - FireFox"
  #   env:
  #       ASPNETCORE_ENVIRONMENT: "Production"
  #       NODE_VERSION: '12.x'                # set this to the node version to use

  #   runs-on: ubuntu-latest

  #   steps:
  #   - uses: actions/checkout@v2.3.4

  #   - name: Trust Certificate
  #     run: |
  #       certPath="$GITHUB_WORKSPACE/Certificates/aspnetapp-root-cert.pfx"
  #       openssl pkcs12 -in "$certPath" -clcerts -nokeys -out "$GITHUB_WORKSPACE/aspnetapp-root-cert.crt" -password pass:password
  #       sudo cp "$GITHUB_WORKSPACE/aspnetapp-root-cert.crt" /usr/local/share/ca-certificates/
  #       sudo update-ca-certificates

  #   - name: Setup Node.js ${{ env.NODE_VERSION }}
  #     uses: actions/setup-node@v1.4.4
  #     with:
  #       node-version: ${{ env.NODE_VERSION }}

  #   - name: Install NET 7
  #     uses: actions/setup-dotnet@v2
  #     with:
  #       dotnet-version: '7.0.x'

  #   - name: Restore Nuget Packages
  #     run: dotnet restore EstateAdministrationUI.sln --source ${{ secrets.PUBLICFEEDURL }} --source ${{ secrets.PRIVATEFEED_URL }}

  #   - name: Build Code
  #     run: |
  #       dotnet build EstateAdministrationUI.sln --configuration ReleaseNoNode
  #       #msbuild.exe EstateAdministrationUI.Javascript.Tests\EstateAdministrationUI.Javascript.Tests.njsproj

  #   - name: Run Unit Tests
  #     run: |
  #       echo "ASPNETCORE_ENVIRONMENT are > ${ASPNETCORE_ENVIRONMENT}"
  #       dotnet test "EstateAdministrationUI.Tests\EstateAdministrationUI.Tests.csproj"
  #       dotnet test "EstateAdministrationUI.BusinessLogic.Tests\EstateAdministrationUI.BusinessLogic.Tests.csproj"
    
  #   - name: Run Javascript Unit Tests
  #     run: |
  #       cd EstateAdministrationUI.Javascript.Tests
  #       npm install
  #       npm run test

  #   - name: Build Docker Image
  #     run: docker build . --file EstateAdministrationUI/Dockerfile --tag estateadministrationui:latest
                
  #   - name: Run Integration Tests (Firefox)
  #     env:
  #       Browser: Firefox
  #     run: dotnet test "EstateAdministrationUI.IntegrationTests\EstateAdministrationUI.IntegrationTests.csproj" --filter Category=PRTest

  #   - uses: actions/upload-artifact@v2
  #     if: ${{ failure() }}
  #     with:
  #       name: firefoxtracelogs
  #       path: /home/txnproc/trace/   

  chrometests:
    name: "Build and Test Pull Requests - Chrome"
    env:
        ASPNETCORE_ENVIRONMENT: "Production"
        NODE_VERSION: '12.x'                # set this to the node version to use

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2.3.4

    - name: Trust Certificate
      run: |
        certPath="$GITHUB_WORKSPACE/Certificates/aspnetapp-root-cert.pfx"
        openssl pkcs12 -in "$certPath" -clcerts -nokeys -out "$GITHUB_WORKSPACE/aspnetapp-root-cert.crt" -password pass:password
        sudo cp "$GITHUB_WORKSPACE/aspnetapp-root-cert.crt" /usr/local/share/ca-certificates/
        sudo update-ca-certificates

    - name: Setup Node.js ${{ env.NODE_VERSION }}
      uses: actions/setup-node@v1.4.4
      with:
        node-version: ${{ env.NODE_VERSION }}

    - name: Install NET 7
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: '7.0.x'

    - name: Restore Nuget Packages
      run: dotnet restore EstateAdministrationUI.sln --source ${{ secrets.PUBLICFEEDURL }} --source ${{ secrets.PRIVATEFEED_URL }}

    - name: Build Code
      run: |
        dotnet build EstateAdministrationUI.sln --configuration ReleaseNoNode
        #msbuild.exe EstateAdministrationUI.Javascript.Tests\EstateAdministrationUI.Javascript.Tests.njsproj

    - name: Run Unit Tests
      run: |
        echo "ASPNETCORE_ENVIRONMENT are > ${ASPNETCORE_ENVIRONMENT}"
        dotnet test "EstateAdministrationUI.Tests\EstateAdministrationUI.Tests.csproj"
        dotnet test "EstateAdministrationUI.BusinessLogic.Tests\EstateAdministrationUI.BusinessLogic.Tests.csproj"
    
    - name: Run Javascript Unit Tests
      run: |
        cd EstateAdministrationUI.Javascript.Tests
        npm install
        npm run test

    - name: Build Docker Image
      run: docker build . --file EstateAdministrationUI/Dockerfile --tag estateadministrationui:latest

    - name: Run Integration Tests (Chrome)
      env:
        Browser: Chrome
      run: dotnet test "EstateAdministrationUI.IntegrationTests\EstateAdministrationUI.IntegrationTests.csproj" --filter Category=PRTest

    - uses: actions/upload-artifact@v2
      if: ${{ failure() }}
      with:
        name: chrometracelogs
        path: /home/txnproc/trace/   

  # edgetests:
  #   name: "Build and Test Pull Requests - Edge"

  #   env:
  #       ASPNETCORE_ENVIRONMENT: "Production"
  #       NODE_VERSION: '12.x'                # set this to the node version to use

  #   runs-on: ubuntu-latest

  #   steps:
  #   - uses: actions/checkout@v2.3.4

  #   - name: Trust Certificate
  #     run: |
  #       certPath="$GITHUB_WORKSPACE/Certificates/aspnetapp-root-cert.pfx"
  #       openssl pkcs12 -in "$certPath" -clcerts -nokeys -out "$GITHUB_WORKSPACE/aspnetapp-root-cert.crt" -password pass:password
  #       sudo cp "$GITHUB_WORKSPACE/aspnetapp-root-cert.crt" /usr/local/share/ca-certificates/
  #       sudo update-ca-certificates

  #   - name: Setup Node.js ${{ env.NODE_VERSION }}
  #     uses: actions/setup-node@v1.4.4
  #     with:
  #       node-version: ${{ env.NODE_VERSION }}
    
  #   - name: Install NET 7
  #     uses: actions/setup-dotnet@v2
  #     with:
  #       dotnet-version: '7.0.x'
    
  #   - name: Restore Nuget Packages
  #     run: dotnet restore EstateAdministrationUI.sln --source ${{ secrets.PUBLICFEEDURL }} --source ${{ secrets.PRIVATEFEED_URL }}

  #   - name: Build Code
  #     run: |
  #       dotnet build EstateAdministrationUI.sln --configuration ReleaseNoNode
  #       #msbuild.exe EstateAdministrationUI.Javascript.Tests\EstateAdministrationUI.Javascript.Tests.njsproj

  #   - name: Run Unit Tests
  #     run: |
  #       echo "ASPNETCORE_ENVIRONMENT are > ${ASPNETCORE_ENVIRONMENT}"
  #       dotnet test "EstateAdministrationUI.Tests\EstateAdministrationUI.Tests.csproj"
  #       dotnet test "EstateAdministrationUI.BusinessLogic.Tests\EstateAdministrationUI.BusinessLogic.Tests.csproj"
    
  #   - name: Run Javascript Unit Tests
  #     run: |
  #       cd EstateAdministrationUI.Javascript.Tests
  #       npm install
  #       npm run test

  #   - name: Build Docker Image
  #     run: docker build . --file EstateAdministrationUI/Dockerfile --tag estateadministrationui:latest

  #   - name: Run Integration Tests (Edge)
  #     env:
  #       Browser: Edge
  #       DriverPath: C:\\SeleniumWebDrivers\\EdgeDriver\\
  #       DriverExe: msedgedriver.exe
  #     run: dotnet test "EstateAdministrationUI.IntegrationTests\EstateAdministrationUI.IntegrationTests.csproj" --filter Category=PRTest

  #   - uses: actions/upload-artifact@v2
  #     if: ${{ failure() }}
  #     with:
  #       name: edgetracelogs
  #       path: /home/txnproc/trace/   
